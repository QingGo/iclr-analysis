# 使用方法

本项目提供 ICLR 论文集的自动化爬取、关键词与优化方向抽取、报告生成的完整流水线。本文档说明命令行用法与 `.env` 环境变量配置。

## 环境准备
- 安装并使用 `uv` 进行依赖管理与运行。
- 首次进入项目目录后执行：
  - `uv sync` 安装依赖并生成本地虚拟环境
  - `cp .env_example .env` 并根据需要填写 `.env`

## 环境变量（.env）
- `YEAR`：分析年份，支持 `2025` 或 `2026`
- `OPENREVIEW_USERNAME`：OpenReview 登录用户名（必填）
- `OPENREVIEW_PASSWORD`：OpenReview 登录密码（必填）
- `OPENAI_BASE_URL`：LLM API Base，可用 DeepSeek/OpenAI 兼容接口（可选）
- `OPENAI_API_KEY`：LLM API Key（可选；不填则跳过 LLM 抽取）
- `OPENAI_MODEL`：LLM 模型名，例如 `deepseek-reasoner`（可选）
- `OPENREVIEW_API_BASE`：OpenReview API Base，默认 `https://api.openreview.net`（可选）
- `USER_AGENT`：HTTP UA 标识，默认 `iclr-analysis/1.0`（可选）

说明：`python-dotenv` 会自动加载 `.env`；当 `YEAR=2026` 时，爬取阶段仅统计评审均分≥6的投稿样本。

## 命令行用法（使用 uv 运行）

### 一键流水线
- 运行完整流程（爬取→抽取→分析）：
  - `uv run python main.py pipeline [选项]`
- 常用选项：
  - `--crawl-limit <int>`：爬取最大数量，默认不限
  - `--crawl-resume/--no-crawl-resume`：是否根据 checkpoint 跳过已完成项，默认启用
  - `--crawl-adaptive/--no-crawl-adaptive`：是否启用自适应并发，默认启用
  - `--crawl-max-concurrency <int>`：爬取最大并发，默认 5（范围 1–10）
  - `--crawl-force-refresh`：强制刷新索引，忽略本地缓存
  - `--extract-limit <int>`：抽取最大数量，默认不限
  - `--extract-resume/--no-extract-resume`：是否根据 checkpoint 跳过已完成项，默认启用
  - `--extract-max-concurrency <int>`：抽取最大并发，默认 5（范围 1–50）

示例：
- `YEAR=2025 uv run python main.py pipeline --crawl-max-concurrency 8 --extract-max-concurrency 8`
- `YEAR=2026 uv run python main.py pipeline --crawl-limit 2000 --crawl-max-concurrency 6`

### 分步骤运行
- 仅爬取：
  - `uv run python crawler.py crawl --limit 1000 --resume --adaptive --max-concurrency 10 --force-refresh`
- 仅抽取：
  - `uv run python extractor.py extract --limit 1000 --resume --max-concurrency 10`
- 仅生成报告：
  - `uv run python analyzer.py`

## 常见工作流
- 运行 2025 全量分析：
  - `YEAR=2025 uv run python main.py pipeline`
- 运行 2026 分析（仅均分≥6样本）：
  - `YEAR=2026 uv run python main.py pipeline`
- 更新抽取与报告（已有爬取数据）：
  - `uv run python extractor.py extract && uv run python analyzer.py`

## 输出目录结构
- `data/{YEAR}/meta/`：论文元数据 JSON
- `data/{YEAR}/raw/`：论文 PDF 原件
- `data/{YEAR}/extracted/`：抽取后的结构化 JSON（含 LLM 标签与优化短语）
- `data/{YEAR}/state/`：`crawl_checkpoint.json` 与 `extract_checkpoint.json`
- `reports/{YEAR}/`：报告产物（`index.html`、`report.md`、`wordcloud.png`、`keywords_top20.html`、`optimizations_top20.html`）

## 说明与建议
- LLM 抽取为可选：未配置 `OPENAI_API_KEY` 时，仅使用元数据与规则抽取。
- 并发与自适应：网络不稳或限流时可降低 `max_concurrency` 并关闭 `adaptive` 以定位问题。
- 2026 样本口径：仅评审均分≥6的投稿计入统计，可能与正式接收集分布不同。

## 问题排查
- OpenReview 认证失败：检查 `OPENREVIEW_USERNAME` 与 `OPENREVIEW_PASSWORD` 是否正确。
- 无 LLM 输出：确保设置 `OPENAI_BASE_URL`、`OPENAI_API_KEY` 与 `OPENAI_MODEL`。
- 抓取异常：尝试 `--crawl-force-refresh` 或降低并发；检查网络代理与 API 可用性。